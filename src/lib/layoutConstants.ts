/**
 * Larghezza artboard sheet dentro il HUD Ray-Ban Display (600×600).
 * Su viewport stretti `FixedArtboard` scala il blocco in modo uniforme.
 */
export const SPARK_ARTBOARD_WIDTH_PX = 512;

/** Viewport fisso Meta Ray-Ban Display Web Apps. */
export const RAYBAN_HUD_SIZE_PX = 600;
